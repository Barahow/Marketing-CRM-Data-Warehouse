name: 'etl_models'
version: '1.0.0'

profile: 'etl_models'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  etl_models:
    marts:
      crm_contacts:
        +materialized: view
      crm_orders:
        +materialized: view
      ga4:
        +materialized: view
      meta:
        +materialized: view
    staging:
      +materialized: view


